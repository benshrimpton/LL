<p id="notice"><%= notice %></p>

<p>
  <strong>Title</strong>
  <%= @page.title %>
  <%= @page.url_key %>
  <%= @page.text_content %>
</p>

<%= link_to 'Edit', edit_page_path(@page) %> |
<%= link_to 'Back', pages_path %>
