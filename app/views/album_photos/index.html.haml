%h1 Listing album_photos

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @album_photos.each do |album_photo|
      %tr
        %td= link_to 'Show', album_photo
        %td= link_to 'Edit', edit_album_photo_path(album_photo)
        %td= link_to 'Destroy', album_photo, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Album photo', new_album_photo_path
